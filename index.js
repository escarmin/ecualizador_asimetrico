import { AppRegistry } from 'react-native';
import App from './App';

// El nombre debe coincidir con getMainComponentName() en MainActivity.kt
const appName = 'AsymmetricEqualizerApp';

AppRegistry.registerComponent(appName, () => App);
